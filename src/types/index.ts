export type User = {
	id: number;
	name: string;
	email: string;
};

export type Product = {
	id: number;
	name: string;
};

export type PurchaseDetail = {
	user_name: string;
	product_name: string;
	purchase_date: string;
};

